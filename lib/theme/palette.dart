import 'package:flutter/material.dart';

class Palette {
  // MINT GREEN (like Mint Cleaning website)
  static const deepGreen = Color(0xFF00B894); // Bright mint green
  static const lightGreen = Color(0xFF55EFC4); // Light mint accent
  
  // BRIGHT VIBRANT RED for headers and hero button
  static const accentRed = Color(0xFFFF4757); // Super bright, vibrant red
  static const accentGold = Color(0xFFD4AF37);
  
  // Backgrounds & text
  static const pageBackground = Color(0xFFFAFAFA);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const textOnLight = Color(0xFF1A1A1A);
  static const textMutedOnLight = Color(0xFF6B6B6B);
  
  // Text colors on dark backgrounds
  static const textOnDark = Color(0xFFFFFFFF);
  static const textMutedOnDark = Color(0xFFE0E0E0);
}